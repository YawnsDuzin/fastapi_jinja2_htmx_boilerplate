# 02. 애플리케이션 진입점 (app/main.py)

## 2.1 파일 개요

`app/main.py`는 FastAPI 애플리케이션의 시작점입니다. 웹 서버(uvicorn)가 이 파일의 `app` 객체를 로드하여 실행합니다.

```
uvicorn app.main:app --reload
        ^^^^^^^^ ^^^
        모듈경로  객체명
```

### 주요 역할

1. FastAPI 애플리케이션 인스턴스 생성
2. 미들웨어 설정 (CORS 등)
3. 라우터 등록 (API, 페이지, 파셜)
4. 정적 파일 서빙 설정
5. 수명주기(lifespan) 관리 (DB 연결/해제)

---

## 2.2 코드 분석

### 2.2.1 Import 섹션

```python
from contextlib import asynccontextmanager
from typing import AsyncGenerator

from fastapi import FastAPI, Request
from fastapi.middleware.cors import CORSMiddleware
from fastapi.responses import HTMLResponse
from fastapi.staticfiles import StaticFiles

from app.api.v1.router import api_router
from app.config import settings
from app.core.exceptions import setup_exception_handlers
from app.core.templates import templates
from app.database import close_db, init_db
from app.pages.router import pages_router
from app.partials.router import partials_router
```

| Import | 용도 |
|--------|------|
| `asynccontextmanager` | 비동기 컨텍스트 매니저 (lifespan 관리) |
| `FastAPI` | 웹 프레임워크 코어 클래스 |
| `CORSMiddleware` | Cross-Origin Resource Sharing 설정 |
| `StaticFiles` | 정적 파일 서빙 |
| `api_router` | REST API 라우터 |
| `pages_router` | HTML 페이지 라우터 |
| `partials_router` | HTMX 파셜 라우터 |

---

### 2.2.2 수명주기(Lifespan) 관리

```python
@asynccontextmanager
async def lifespan(app: FastAPI) -> AsyncGenerator:
    """
    애플리케이션 수명주기 관리
    """
    # =========================================================================
    # Startup (앱 시작 시 실행)
    # =========================================================================
    print("🚀 애플리케이션 시작 중...")
    await init_db()  # DB 엔진 생성 및 테이블 초기화
    print("✅ 데이터베이스 초기화 완료")

    yield  # 앱이 실행되는 동안 여기서 대기

    # =========================================================================
    # Shutdown (앱 종료 시 실행)
    # =========================================================================
    print("🛑 애플리케이션 종료 중...")
    await close_db()  # DB 연결 풀 정리
    print("✅ 데이터베이스 연결 종료 완료")
```

#### 동작 원리

```
서버 시작
    │
    ▼
┌─────────────────────────────┐
│ yield 이전 코드 실행        │  ← Startup
│ (DB 초기화, 캐시 설정 등)   │
└─────────────────────────────┘
    │
    ▼
┌─────────────────────────────┐
│ yield (앱이 요청 처리)      │  ← 서버 실행 중
└─────────────────────────────┘
    │
    ▼ (종료 신호)
┌─────────────────────────────┐
│ yield 이후 코드 실행        │  ← Shutdown
│ (DB 연결 해제, 정리)        │
└─────────────────────────────┘
```

> **참고**: 이전 방식의 `@app.on_event("startup")` / `@app.on_event("shutdown")`은 deprecated 되었으며, lifespan 방식이 권장됩니다.

---

### 2.2.3 애플리케이션 팩토리 패턴

```python
def create_app() -> FastAPI:
    """
    FastAPI 애플리케이션 팩토리
    """
    # =========================================================================
    # FastAPI 인스턴스 생성
    # =========================================================================
    app = FastAPI(
        title=settings.app_name,                    # API 문서 제목
        description="FastAPI + Jinja2 + HTMX",      # API 설명
        version="1.0.0",                            # API 버전
        docs_url="/docs" if settings.debug else None,      # Swagger UI
        redoc_url="/redoc" if settings.debug else None,    # ReDoc
        openapi_url="/openapi.json" if settings.debug else None,
        lifespan=lifespan,                          # 수명주기 관리자
    )
```

#### 팩토리 패턴의 장점

1. **테스트 용이성**: 다른 설정으로 앱 인스턴스 생성 가능
2. **유연성**: 환경별로 다른 구성 적용 가능
3. **재사용성**: 여러 앱 인스턴스 생성 가능

#### API 문서 설정

| 속성 | 값 | 설명 |
|------|-----|------|
| `docs_url` | `/docs` 또는 `None` | Swagger UI 경로 |
| `redoc_url` | `/redoc` 또는 `None` | ReDoc 경로 |
| `openapi_url` | `/openapi.json` 또는 `None` | OpenAPI 스키마 경로 |

> **보안**: 프로덕션 환경(`debug=False`)에서는 API 문서가 비활성화됩니다.

---

### 2.2.4 CORS 미들웨어 설정

```python
app.add_middleware(
    CORSMiddleware,
    allow_origins=settings.cors_origins,  # 허용할 도메인 목록
    allow_credentials=True,               # 쿠키 전송 허용
    allow_methods=["*"],                  # 모든 HTTP 메서드 허용
    allow_headers=["*"],                  # 모든 헤더 허용
)
```

#### CORS란?

CORS(Cross-Origin Resource Sharing)는 다른 도메인에서 이 API에 접근할 수 있도록 허용하는 보안 메커니즘입니다.

```
┌────────────────────────┐     HTTP 요청      ┌────────────────────────┐
│ 프론트엔드             │ ──────────────────► │ 백엔드                 │
│ localhost:3000         │                     │ localhost:8001         │
│                        │ ◄────────────────── │                        │
│ (다른 도메인)          │     CORS 응답       │ (이 서버)              │
└────────────────────────┘                     └────────────────────────┘
```

#### 주요 옵션

| 옵션 | 값 | 설명 |
|------|-----|------|
| `allow_origins` | `["http://localhost:3000"]` | 허용할 출처 목록 |
| `allow_credentials` | `True` | 쿠키 전송 허용 (JWT 인증에 필수) |
| `allow_methods` | `["*"]` | 허용할 HTTP 메서드 |
| `allow_headers` | `["*"]` | 허용할 HTTP 헤더 |

---

### 2.2.5 정적 파일 마운트

```python
app.mount("/static", StaticFiles(directory="static"), name="static")
```

#### 동작 방식

```
요청: GET /static/css/custom.css
              │
              ▼
┌─────────────────────────────────┐
│ StaticFiles 미들웨어           │
│ 경로: /static → static/ 디렉토리 │
└─────────────────────────────────┘
              │
              ▼
응답: static/css/custom.css 파일 내용
```

---

### 2.2.6 예외 핸들러 설정

```python
setup_exception_handlers(app)
```

`app/core/exceptions.py`에서 정의된 전역 예외 핸들러를 등록합니다:

- `404 Not Found` → 404 페이지 렌더링
- `500 Internal Server Error` → 500 페이지 렌더링
- `AppException` (커스텀 예외) → 적절한 에러 응답

---

### 2.2.7 라우터 등록

```python
# API 라우터: /api/v1 프리픽스로 등록
app.include_router(api_router, prefix="/api/v1")

# 페이지 라우터: 프리픽스 없이 루트에 등록
app.include_router(pages_router)

# 파셜 라우터: /partials 프리픽스로 등록
app.include_router(partials_router, prefix="/partials")
```

#### 라우터 URL 매핑

| 라우터 | 프리픽스 | 예시 URL |
|--------|----------|----------|
| `api_router` | `/api/v1` | `/api/v1/auth/login`, `/api/v1/items` |
| `pages_router` | (없음) | `/`, `/login`, `/dashboard` |
| `partials_router` | `/partials` | `/partials/items`, `/partials/toasts/success` |

---

### 2.2.8 앱 인스턴스 생성

```python
app = create_app()
```

이 변수가 uvicorn이 찾는 진입점입니다.

---

### 2.2.9 헬스 체크 엔드포인트

```python
@app.get("/health", tags=["health"])
async def health_check():
    """
    헬스 체크 엔드포인트
    """
    return {"status": "healthy", "app": settings.app_name}
```

#### 사용처

- 로드밸런서의 헬스 체크
- 쿠버네티스 liveness/readiness probe
- 모니터링 시스템

#### 응답 예시

```json
{
    "status": "healthy",
    "app": "FastAPI-HTMX-Boilerplate"
}
```

---

## 2.3 전체 흐름 다이어그램

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        uvicorn 서버 시작                                │
│                     uvicorn app.main:app                                │
└─────────────────────────────────────────────────────────────────────────┘
                                  │
                                  ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                      create_app() 함수 호출                             │
└─────────────────────────────────────────────────────────────────────────┘
                                  │
                                  ▼
┌─────────────────────────────────────────────────────────────────────────┐
│  1. FastAPI 인스턴스 생성 (lifespan 연결)                               │
│  2. CORS 미들웨어 추가                                                  │
│  3. 정적 파일 마운트 (/static)                                          │
│  4. 예외 핸들러 등록                                                    │
│  5. 라우터 등록 (API, Pages, Partials)                                  │
└─────────────────────────────────────────────────────────────────────────┘
                                  │
                                  ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                      lifespan - Startup                                 │
│                  • init_db() - DB 테이블 생성                           │
└─────────────────────────────────────────────────────────────────────────┘
                                  │
                                  ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                      서버 실행 (요청 대기)                               │
│                  http://localhost:8001                                  │
└─────────────────────────────────────────────────────────────────────────┘
                                  │
                               (종료 시)
                                  ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                      lifespan - Shutdown                                │
│                  • close_db() - DB 연결 해제                            │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 2.4 설정 파일 참조

`app/config.py`에서 다음 설정들이 사용됩니다:

| 설정 | 용도 |
|------|------|
| `settings.app_name` | API 문서 제목 |
| `settings.debug` | 개발 모드 여부 (API 문서 활성화) |
| `settings.cors_origins` | CORS 허용 도메인 |
| `settings.database_url` | 데이터베이스 연결 문자열 |
